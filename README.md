Skill 自动处理的内容
连接你的 Chrome（通过 CDP 端口）
搜索 IEEE 并自动应用年份筛选 + 被引量排序
提取论文列表（标题、链接、年份）
下载 PDF — 优先从 IEEE 直接下载；需要订阅的自动通过 Sci-Hub 回退
保存结果 — PDF + papers_list.json 到指定文件夹
之前下载的那 3 篇 PDF 和 papers_list.json 仍然在 IEEE/ 文件夹下。
